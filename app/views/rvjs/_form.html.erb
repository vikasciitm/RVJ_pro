<%= form_with(model: rvj) do |form| %>
  <% if rvj.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rvj.errors.count, "error") %> prohibited this rvj from being saved:</h2>

      <ul>
        <% rvj.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :block, style: "display: block" %>
    <%= form.text_field :block %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
